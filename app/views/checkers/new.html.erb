<% provide(:meta_description, 'Tool to find geolocation and other information about any IP v4 and v6 address.') %>
<%= render 'components/layout-default/layout-default' do %>
  <%= render 'components/checker/checker', checker: @checker, checker_json: @checker_json %>
<% end %>
